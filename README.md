# tic-tac-toe

Rails app. Run it:

```
# Start database:
docker-compose up

# Run migrations
rails db:create
rails db:migrate

# fetch gems
bundle

# start server
rails s
```

Run tests:

```
bundle exec rspec
```

